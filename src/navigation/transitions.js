// Vertical slide transition - slides up from bottom (modern and smooth)
export const verticalSlideTransition = {
  animation: 'slide_from_bottom',
};

// Fade transition - smooth fade in/out
export const fadeTransition = {
  animation: 'fade',
};

// Flip transition - creative 3D flip effect
export const flipTransition = {
  animation: 'flip',
};

